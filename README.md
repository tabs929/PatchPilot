# PatchPilot

An autonomous agent that takes a GitHub issue and a repo, locates the bug, writes a multi-file fix, and self-corrects against the test suite until it passes — benchmarked on SWE-bench Verified.
